#include <iostream>
#include <windows.h>
#include <mysql.h>


#include "people.h"
#include "student.h"
int main()
{student s1;

    /*
    std::cout<<"who are you\n\n"
            <<"(1)people\n"
            <<"(2)student\n"
            <<"(3)teacher\n"
            <<"your choose:";
    int user;

    std::cin>>user;

    if(user == 1)
   {
      people p1;
   }

    else if( user == 2)
    {
        student s1;

    }

    else if(user == 3)
    {
              std::cout<<"success\n";

    }
    else
      std::cout<<"fail\n";
*/
    return 0;


}


/*

MYSQL *conn;

    conn = mysql_init(0);

    conn = mysql_real_connect(conn , "localhost" , "root" , "timmy001" , "myschool" , 0 , NULL  ,0);

    if(!conn)
        std::cout<<"fail\n";


*/
